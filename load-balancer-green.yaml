apiVersion: v1
kind: Service
metadata:
    name: loadbalancer-green
spec:
    type: LoadBalancer
    selector:
        app: capstone-app
        version: "VERSION"
    ports:
    - protocol: TCP
      port: 5000
      targetPort: 80
    
    